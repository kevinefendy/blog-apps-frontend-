import 'package:flutter/material.dart';

class MyBlogPage extends StatelessWidget {
  const MyBlogPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("MyBlog belum dipakai")),
    );
  }
}
